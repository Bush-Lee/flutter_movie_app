name: movie_app
description: A new Flutter application.

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+2
  path_provider: ^1.4.5
  equatable: ^0.3.0
  flutter_bloc: ^0.19.0
  video_player: ^0.10.6
  # Someday you will use this
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/movie_genre/
    - assets/images/movie_genre/file_names.txt